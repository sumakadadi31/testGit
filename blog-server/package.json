{
  "name": "blog-server",
  "version": "1.0.0",
  "description": "json-server based server to serve and update blog posts",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "json:server": "json-server --watch db.json"
  },
  "author": "Prashanth Puranik",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.12.1"
  }
}